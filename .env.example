# Environment Configuration for Vercel Deployment

# Server Configuration
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# Database (if using database in future)
# DATABASE_URL=your_database_url_here

# Session Secret (change this for production)
SESSION_SECRET=your_secret_key_here

# Admin Credentials (these are set in storage.ts but you can override them here if needed)
# ADMIN_USERNAME=promax
# ADMIN_PASSWORD=promax@69
